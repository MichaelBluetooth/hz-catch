import { FilterBtnLabelPipe } from './filter-btn-label.pipe';

describe('FilterBtnLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBtnLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
