import { CountSummaryPipe } from './count-summary.pipe';

describe('CountSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new CountSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
